// import { DatePicker } from "@material-ui/pickers";
// import { DateTime } from "luxon";

// const component = (props) => {
//   return (
//     <DatePicker
//       autoOk
//       format={"MMM-y"}
//       fullWidth={props.fullWidth}
//       inputRef={props.ref}
//       label={props.label}
//       name={props.name}
//       onBlur={props.onBlur}
//       onChange={(v) => props.onChange(v.toISODate())}
//       value={DateTime.fromISO(props.value || DateTime.now().toISODate()) }
//       defaultValue={props.defaultValue}
//     />
//   );
// };

// export default component;
